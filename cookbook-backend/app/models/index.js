const recipe = require('./recipe');
const ingredient = require('./Ingredient');
const ingredientDetail = require('./ingredient_detail');

module.exports = {
    recipe,
    ingredient,
    ingredientDetail,
}